UI frontend that allows to manage the persistent provisionerd daemon. This is a outside program that is available to be deployed on the same machine as the daemon but accessible to people who need it.

This will be developed after the daemon and `provctl` are finished.
